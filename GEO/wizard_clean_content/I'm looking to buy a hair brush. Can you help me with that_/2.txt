The provided document does not contain the main text content from the scraped web page. Instead, it shows a security verification message indicating that access to the URL (Redken's blog post about finding the best hairbrush for your hair type) is restricted due to a 403 Forbidden error. The page is likely protected by a security check (e.g., CAPTCHA or Cloudflare), preventing direct scraping of the content.

To proceed:
1. Check if the URL requires manual verification (e.g., solving a CAPTCHA).  
2. Ensure proper permissions or headers (e.g., user-agent) are included in the scraping request.  
3. Alternatively, visit the page manually to retrieve the intended hair-care advice.  

No extractable main text is available in the current response.